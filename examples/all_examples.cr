# Type-checks every README sample in one compilation (`scripts/check.sh`).
#
# The samples themselves live one per file so each reads exactly as it does in the README;
# nothing here is meant to be run, only compiled.
require "./usage"
require "./item_operations"
require "./batch_read"
require "./batch_write"
require "./transactions"
require "./inheritance"
require "./lucky_app"
