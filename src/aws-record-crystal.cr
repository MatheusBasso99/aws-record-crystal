# aws-record-crystal — object mapping abstraction for Amazon DynamoDB.
#
# This is the shard entry point; it exists so that `require "aws-record-crystal"`
# (the shard name) works. Everything lives under the `Aws` namespace.
require "./aws-record"
